import 'package:jonyintlbd/utils/size_config.dart';

class SpaceUtils{
  SpaceUtils();

  double fullHeight = SizeConfig.blockSizeVertical! * 100;
  double fullWidth = SizeConfig.blockSizeHorizontal! * 100;
  double bannerHorizontalPadding = SizeConfig.blockSizeHorizontal! * 5;
  double sectionHorizontalPadding = SizeConfig.blockSizeHorizontal! * 8;
  double sectionVerticalPadding = SizeConfig.blockSizeVertical! * 10;
  double elementVerticalSpace = SizeConfig.blockSizeVertical! * 3;

}